import 'package:flutter/cupertino.dart';

class SideDialog extends StatelessWidget {
  const SideDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
